apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - certificate.yaml

patches:
  - path: apiserver-proxy-patch.yaml
    target:
      kind: Deployment
      name: milo-apiserver
      namespace: milo-system
